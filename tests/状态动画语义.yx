引「../src/核心/状态.yx」为 状态；

引「标准:测试」为 测试；

令 通知次数 为 0；

令 最后值 为 0；

法 观察（新值，旧值） 则
    置 通知次数 为 （通知次数 加 1）；
    置 最后值 为 新值；
终

定 所值 为 状态.可观察值（1）；

定 订阅编号 为 所值.订阅（观察，假）；

所值.设值（2）；

所值.设值（2）；

测试.相等（通知次数，1）；

测试.相等（最后值，2）；

所值.取消订阅（订阅编号）；

所值.设值（3）；

测试.相等（通知次数，1）；

定 表 为 状态.状态表（{「标题」：「初始」}）；

测试.相等（表.取（「标题」，「」），「初始」）；

令 绑定值 为 「」；

法 应用值（新值，旧值） 则
    置 绑定值 为 新值；
终

法 加前缀（值） 则
    归 （「标题:」 加 值）；
终

定 所绑定 为 状态.绑定（表.观察（「标题」），应用值，加前缀）；

测试.相等（绑定值，「标题:初始」）；

表.设值（「标题」，「更新」）；

测试.相等（绑定值，「标题:更新」）；

所绑定.关闭（）；

表.设值（「标题」，「忽略」）；

测试.相等（绑定值，「标题:更新」）；

定 所动画 为 状态.动画（0，100，10，1000，「缓入缓出」）；

测试.相等（所动画.值（10），0）；

测试.相等（所动画.值（10.5），50）；

测试.相等（所动画.值（11），100）；

测试.断言（非 所动画.运行中，「到达终点后动画应停止」）；

定 根语义 为 状态.语义节点（「窗口」，「示例」）；

定 按钮语义 为 状态.语义节点（「按钮」，「保存」）；

置 根语义.编号 为 1；

置 按钮语义.编号 为 2；

置 按钮语义.操作 为 【「点击」】；

根语义.加子（按钮语义）；

定 语义快照 为 根语义.快照（）；

测试.相等（语义快照【「编号」】，1）；

测试.相等（语义快照【「子」】【0】【「编号」】，2）；

测试.相等（语义快照【「子」】【0】【「角色」】，「按钮」）；

测试.相等（语义快照【「子」】【0】【「操作」】【0】，「点击」）；

言 「状态动画与语义测试通过」；

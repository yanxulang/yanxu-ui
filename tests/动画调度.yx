引「../src/核心/动画.yx」为 动画；

引「标准:数学」为 数学；

引「标准:测试」为 测试；

令 最后值 为 减 1；

令 更新次数 为 0；

令 完成次数 为 0；

令 回调内队列：任意 为 空；

令 回调内取消阈值 为 0；

法 记录更新（所值） 则
    置 最后值 为 所值；
    置 更新次数 为 （更新次数 加 1）；
终

法 记录完成（） 则
    置 完成次数 为 （完成次数 加 1）；
终

法 回调内取消（所值） 则
    若 （（回调内队列 不等于 空） 且 （所值 不小于 回调内取消阈值）） 则
        回调内队列.取消全部（）；
    终
终

定 队列 为 动画.动画队列（2）；

定 首任务 为 队列.开始（{「起值」：0，「终值」：100，「时长毫秒」：1000，「缓动」：「线性」}，10，记录更新，记录完成）；

测试.相等（首任务.编号（），1）；

测试.相等（最后值，0）；

测试.相等（更新次数，1）；

测试.相等（队列.快照（）【「活动」】，1）；

测试.断言（队列.推进（10.5），「中途推进后动画仍应活动」）；

测试.断言（（数学.绝对值（（最后值 减 50）） 小于 0.000001），「线性动画中点必须正确」）；

测试.断言（非 队列.推进（11），「到达终点后队列应为空」）；

测试.相等（最后值，100）；

测试.相等（完成次数，1）；

测试.相等（队列.快照（）【「完成总数」】，1）；

定 取消任务 为 队列.开始（{「起值」：10，「终值」：20，「时长毫秒」：2000，「缓动」：「缓出」}，20，记录更新，空）；

测试.断言（队列.取消（取消任务.编号（）），「活动动画应可取消」）；

测试.相等（取消任务.状态（），「已取消」）；

测试.相等（队列.快照（）【「取消总数」】，1）；

定 直接取消任务 为 队列.开始（{「起值」：5，「终值」：15，「时长毫秒」：500}，25，记录更新，空）；

测试.断言（直接取消任务.取消（），「任务句柄应可直接取消」）；

测试.相等（队列.快照（）【「取消总数」】，2）；

测试.相等（队列.快照（）【「活动」】，0）；

置 回调内队列 为 动画.动画队列（2）；

置 回调内取消阈值 为 1；

定 初始回调取消任务 为 回调内队列.开始（{「起值」：1，「终值」：2，「时长毫秒」：1000}，0，回调内取消，空）；

测试.相等（初始回调取消任务.状态（），「已取消」）；

测试.相等（回调内队列.快照（）【「活动」】，0）；

测试.相等（回调内队列.快照（）【「取消总数」】，1）；

置 回调内队列 为 动画.动画队列（2）；

置 回调内取消阈值 为 0.5；

定 推进回调取消任务 为 回调内队列.开始（{「起值」：0，「终值」：1，「时长毫秒」：1000}，0，回调内取消，空）；

测试.断言（非 回调内队列.推进（0.5），「推进回调应能安全取消所属队列」）；

测试.相等（推进回调取消任务.状态（），「已取消」）；

测试.相等（回调内队列.快照（）【「取消总数」】，1）；

法 失败更新（所值） 则
    抛 「TEST_ANIMATION_CALLBACK|预期失败」；
终

定 失败任务 为 队列.开始（{「起值」：0，「终值」：1，「时长毫秒」：10}，30，失败更新，空）；

测试.相等（失败任务.状态（），「失败」）；

测试.相等（队列.快照（）【「失败总数」】，1）；

测试.相等（队列.快照（）【「活动」】，0）；

定 小队列 为 动画.动画队列（1）；

小队列.开始（{「时长毫秒」：1000}，40，记录更新，空）；

令 捕获上限 为 假；

试 则
    小队列.开始（{「时长毫秒」：1000}，40，记录更新，空）；
救 所误 则
    置 捕获上限 为 真；
终

测试.断言（捕获上限，「活动动画超过上限必须失败」）；

令 捕获时长 为 假；

试 则
    动画.动画任务（99，{「时长毫秒」：0}，0，记录更新，空）；
救 所误 则
    置 捕获时长 为 真；
终

测试.断言（捕获时长，「无效动画时长必须失败」）；

测试.相等（小队列.取消全部（），1）；

测试.相等（小队列.快照（）【「活动」】，0）；

言 「动画调度测试通过」；

引「../src/控件/叠层实现.yx」为 叠层；

引「标准:测试」为 测试；

引「标准:数学」为 数学；

类 假叠层 则
    公 域 标识：数；
    公 域 锚框：列<数>；
    公 域 所需尺寸：列<数>；
    公 域 配置值：典；
    公 域 框：列<数>；
    公 域 实际方向：文；
    公 域 布局次数：数；
    公 法 初始化（标识：数，锚框：列<数>，所需尺寸：列<数>，配置值：典） 则
        置 此.标识 为 标识；
        置 此.锚框 为 锚框；
        置 此.所需尺寸 为 所需尺寸；
        置 此.配置值 为 配置值；
        置 此.框 为 【0，0，0，0】；
        置 此.实际方向 为 「」；
        置 此.布局次数 为 0；
    终
    公 法 编号（）：数 则
        归 此.标识；
    终
    公 法 叠层锚框（）：列<数> 则
        归 此.锚框；
    终
    公 法 叠层尺寸（）：列<数> 则
        归 此.所需尺寸；
    终
    公 法 叠层配置（）：典 则
        归 此.配置值；
    终
    公 法 叠层布局（新框：列<数>，方向：文） 则
        置 此.框 为 新框；
        置 此.实际方向 为 方向；
        置 此.布局次数 为 （此.布局次数 加 1）；
    终
    公 法 叠层命中（横坐标：数，纵坐标：数） 则
        若 （（（（横坐标 不小于 此.框【0】） 且 （纵坐标 不小于 此.框【1】）） 且 （横坐标 小于 （此.框【0】 加 此.框【2】））） 且 （纵坐标 小于 （此.框【1】 加 此.框【3】））） 则
            归 此；
        终
        归 空；
    终
终

法 框近似（实际，期望，名称：文） 则
    逐 索 于 范围（0，长度（期望）） 则
        测试.断言（（数学.绝对值（（实际【索】 减 期望【索】）） 小于 0.000001），（名称 加 「坐标应一致」））；
    终
终

定 栈 为 叠层.叠层栈（【0，0，320，240】）；

定 甲 为 假叠层（1，【20，20，60，30】，【100，60】，{「方向」：「下」}）；

定 乙 为 假叠层（2，【40，40，60，30】，【100，60】，{「方向」：「下」}）；

测试.断言（栈.打开（甲），「首次打开应登记新叠层」）；

测试.断言（栈.打开（乙），「第二层应登记成功」）；

测试.相等（栈.数量（），2）；

测试.相等（栈.顶层（）.编号（），2）；

框近似（甲.框，【20，54，100，60】，「甲层」）；

测试.相等（栈.命中（50，80）.编号（），2）；

测试.断言（非 栈.打开（甲），「重复打开不应增加数量」）；

测试.相等（栈.数量（），2）；

测试.相等（栈.顶层（）.编号（），1）；

定 外部关闭 为 栈.外部关闭列（300，220）；

测试.相等（长度（外部关闭），2）；

测试.相等（外部关闭【0】.编号（），1）；

测试.相等（外部关闭【1】.编号（），2）；

置 甲.锚框 为 【20，70，60，20】；

栈.设置视口（【0，0，140，100】）；

测试.相等（甲.实际方向，「上」）；

测试.断言（（甲.布局次数 大于 1），「视口变化应重新布局」）；

测试.断言（栈.移除（甲），「移除已登记叠层应返回真」）；

测试.断言（非 栈.移除（甲），「重复移除应幂等返回假」）；

定 模态 为 假叠层（3，【20，20，60，30】，【80，50】，{「方向」：「下」，「模态」：真，「外部关闭」：假}）；

栈.打开（模态）；

测试.断言（栈.阻止背景（130，5），「模态层应阻止框外背景交互」）；

测试.断言（非 栈.阻止背景（30，60），「模态层框内应允许交互」）；

测试.相等（长度（栈.外部关闭列（130，5）），0）；

定 子层 为 假叠层（4，【80，50，20，20】，【50，40】，{「方向」：「右」}）；

栈.打开（子层）；

测试.断言（非 栈.阻止背景（65，55），「模态层之上的子层区域应允许交互」）；

定 小栈 为 叠层.叠层栈（【0，0，100，100】）；

置 小栈.上限 为 1；

小栈.打开（假叠层（10，【0，0，10，10】，【20，20】，{}））；

令 捕获上限 为 假；

试 则
    小栈.打开（假叠层（11，【0，0，10，10】，【20，20】，{}））；
救 所误 则
    置 捕获上限 为 真；
终

测试.断言（捕获上限，「叠层数量超过上限必须失败」）；

言 「叠层栈测试通过」；

引「../src/事件/路由.yx」为 事件；

引「标准:测试」为 测试；

类 假节点 则
    公 域 标识：文；
    公 域 父项：任意；
    公 域 子列：列；
    公 域 日志：列；
    公 域 可聚焦：理；
    公 域 聚焦中：理；
    公 法 初始化（标识：文，父值，日志：列，可聚焦：理） 则
        置 此.标识 为 标识；
        置 此.父项 为 父值；
        置 此.子列 为 【】；
        置 此.日志 为 日志；
        置 此.可聚焦 为 可聚焦；
        置 此.聚焦中 为 假；
        若 （父值 不等于 空） 则
            追加（父值.子列，此）；
        终
    终
    公 法 编号（）：文 则
        归 此.标识；
    终
    公 法 父节点（） 则
        归 此.父项；
    终
    公 法 接收事件（所事件，阶段：文） 则
        追加（此.日志，（（阶段 加 「:」） 加 此.标识））；
    终
    公 法 允许焦点（）：理 则
        归 此.可聚焦；
    终
    公 法 设置焦点状态（状态：理） 则
        置 此.聚焦中 为 状态；
    终
    公 法 收集可聚焦（结果：列） 则
        若 此.允许焦点（） 则
            追加（结果，此）；
        终
        逐 子 于 此.子列 则
            子.收集可聚焦（结果）；
        终
    终
终

定 日志：列 为 【】；

定 根 为 假节点（「1」，空，日志，假）；

定 中节点 为 假节点（「2」，根，日志，真）；

定 目标 为 假节点（「3」，中节点，日志，真）；

定 路由 为 事件.路由器（）；

定 所事件 为 路由.发送（目标，{「类型」：「指针按下」，「未来字段」：42}）；

测试.相等（长度（日志），5）；

测试.相等（日志【0】，「捕获:1」）；

测试.相等（日志【1】，「捕获:2」）；

测试.相等（日志【2】，「目标:3」）；

测试.相等（日志【3】，「冒泡:2」）；

测试.相等（日志【4】，「冒泡:1」）；

测试.相等（所事件.字段（「未来字段」，0），42）；

测试.相等（所事件.字段（「不存在」，7），7）；

定 焦点 为 事件.焦点管理器（根，路由）；

测试.断言（焦点.移动（假），「Tab 应聚焦首项」）；

测试.断言（中节点.聚焦中，「父节点应获得焦点」）；

测试.断言（焦点.移动（假），「再次 Tab 应聚焦下一项」）；

测试.断言（（目标.聚焦中 且 非 中节点.聚焦中），「目标应获得焦点」）；

测试.断言（焦点.移动（真），「Shift+Tab 应反向移动」）；

测试.断言（中节点.聚焦中，「反向移动应回到父节点」）；

定 模态根 为 假节点（「4」，根，日志，假）；

定 模态甲 为 假节点（「5」，模态根，日志，真）；

定 模态乙 为 假节点（「6」，模态根，日志，真）；

定 嵌套根 为 假节点（「7」，模态根，日志，假）；

定 嵌套项 为 假节点（「8」，嵌套根，日志，真）；

测试.断言（焦点.进入范围（模态根），「进入模态范围应成功」）；

测试.相等（焦点.范围深度（），1）；

测试.断言（模态甲.聚焦中，「模态范围应自动聚焦首项」）；

测试.断言（非 焦点.请求（目标），「活动模态范围应拒绝域外焦点」）；

测试.断言（焦点.移动（假），「模态范围内应可向前移动」）；

测试.断言（模态乙.聚焦中，「第二个模态项目应获得焦点」）；

测试.断言（焦点.移动（假），「模态范围应包含嵌套后代」）；

测试.断言（嵌套项.聚焦中，「嵌套后代应参与外层 Tab 顺序」）；

测试.断言（焦点.移动（假），「模态范围应循环焦点」）；

测试.断言（模态甲.聚焦中，「Tab 应在模态范围内回到首项」）；

测试.断言（焦点.进入范围（模态根），「重复进入同一范围应幂等」）；

测试.相等（焦点.范围深度（），1）；

测试.断言（焦点.进入范围（嵌套根），「应允许进入当前范围内的嵌套范围」）；

测试.相等（焦点.范围深度（），2）；

测试.断言（嵌套项.聚焦中，「嵌套范围应聚焦自己的首项」）；

测试.断言（非 焦点.退出范围（模态根），「不能越过顶层范围关闭外层」）；

测试.断言（焦点.退出范围（嵌套根），「退出嵌套范围应成功」）；

测试.断言（模态甲.聚焦中，「退出嵌套范围应恢复原焦点」）；

测试.断言（焦点.退出范围（模态根），「退出模态范围应成功」）；

测试.相等（焦点.范围深度（），0）；

测试.断言（中节点.聚焦中，「关闭模态范围应恢复进入前焦点」）；

令 快捷次数 为 0；

法 保存（原始） 则
    置 快捷次数 为 （快捷次数 加 1）；
终

定 快捷 为 事件.快捷键表（）；

快捷.绑定（「主+s」，保存）；

测试.断言（快捷.处理（{「逻辑键」：「S」，「修饰键」：{「主修饰」：真}}），「快捷键应命中」）；

测试.相等（快捷次数，1）；

测试.断言（非 快捷.处理（{「逻辑键」：「P」，「修饰键」：{「主修饰」：真}}），「未绑定快捷键不应命中」）；

言 「事件与焦点测试通过」；

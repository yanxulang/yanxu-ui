引「../src/核心/生命周期.yx」为 生命周期；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

公 类 假平台计时器 则
    公 域 编号值：数；
    公 域 取消数：数；
    公 域 关闭数：数；
    公 法 初始化（编号值：数） 则
        置 此.编号值 为 编号值；
        置 此.取消数 为 0；
        置 此.关闭数 为 0；
    终
    公 法 编号（）：数 则
        归 此.编号值；
    终
    公 法 取消（） 则
        置 此.取消数 为 （此.取消数 加 1）；
    终
    公 法 关闭（） 则
        置 此.关闭数 为 （此.关闭数 加 1）；
    终
终

公 类 假平台图片 则
    公 域 句柄：数；
    公 域 关闭数：数；
    公 域 查询失败：理；
    公 域 关闭失败：理；
    公 法 初始化（句柄值：数，查询失败：理，关闭失败：理） 则
        置 此.句柄 为 句柄值；
        置 此.关闭数 为 0；
        置 此.查询失败 为 查询失败；
        置 此.关闭失败 为 关闭失败；
    终
    公 法 查询（）：典 则
        若 此.查询失败 则
            抛 「TEST_IMAGE_QUERY|预期查询失败」；
        终
        归 {「宽」：2，「高」：1，「字节数」：8}；
    终
    公 法 关闭（） 则
        置 此.关闭数 为 （此.关闭数 加 1）；
        若 此.关闭失败 则
            抛 「TEST_IMAGE_CLOSE|预期关闭失败」；
        终
    终
终

公 类 已关闭平台计时器 则
    公 法 编号（）：数 则
        归 41；
    终
    公 法 取消（） 则
        抛 「[PLATFORM_RESOURCE_CLOSED] ABI v2 参数无效」；
    终
    公 法 关闭（） 则
        抛 「[PLATFORM_RESOURCE_CLOSED] ABI v2 参数无效」；
    终
终

公 类 已关闭平台图片 则
    公 域 句柄：数；
    公 法 初始化（） 则
        置 此.句柄 为 42；
    终
    公 法 查询（）：典 则
        归 {「宽」：1，「高」：1，「字节数」：4}；
    终
    公 法 关闭（） 则
        抛 「[PLATFORM_RESOURCE_CLOSED] ABI v2 参数无效」；
    终
终

令 移除总数 为 0；

令 回调总数 为 0；

令 图片移除总数 为 0；

令 关闭步骤总数 为 0；

法 记录移除（任务值） 则
    置 移除总数 为 （移除总数 加 1）；
终

法 记录触发（原始） 则
    置 回调总数 为 （回调总数 加 1）；
终

法 失败触发（原始） 则
    抛 「TEST_TIMER_CALLBACK|预期失败」；
终

法 记录图片移除（资源值） 则
    置 图片移除总数 为 （图片移除总数 加 1）；
终

法 成功关闭步骤（） 则
    置 关闭步骤总数 为 （关闭步骤总数 加 1）；
终

法 失败关闭步骤（） 则
    抛 「TEST_CLOSE_STEP|预期关闭失败」；
终

法 已关闭步骤（） 则
    抛 「[PLATFORM_RESOURCE_CLOSED] ABI v2 参数无效」；
终

定 硬上限：典 为 {「资源总数」：4096，「窗口数」：64，「计时器数」：2048，「图片数」：256，「字体数」：128，「图片字节」：1073741824，「字体字节」：536870912，「帧字节」：1073741824，「无障碍节点」：1048576，「无障碍文字字节」：268435456}；

定 能力：典 为 {「应用资源配额」：真，「应用资源配额可下调」：真，「应用资源配额拒绝统计」：真，「应用退出幂等」：真，「应用生命周期统计」：真，「应用关闭资源归零」：真，「应用生命周期状态」：【「就绪」，「运行中」，「退出请求」，「已退出」，「已关闭」】，「应用资源硬上限」：硬上限}；

定 结果 为 生命周期.资源能力（能力）；

测试.断言（结果【「支持」】，「有效言台能力应通过协商」）；

测试.相等（结果【「硬上限」】【「窗口数」】，64）；

测试.相等（结果【「生命周期状态」】【4】，「已关闭」）；

置 结果【「硬上限」】【「窗口数」】 为 1；

测试.相等（生命周期.资源能力（能力）【「硬上限」】【「窗口数」】，64）；

令 捕获缺失能力 为 假；

试 则
    生命周期.资源能力（{「应用资源配额」：假}）；
救 所误 则
    置 捕获缺失能力 为 真；
终

测试.断言（捕获缺失能力，「缺失必需能力必须失败」）；

令 捕获错误上限 为 假；

置 能力【「应用资源硬上限」】【「窗口数」】 为 1.5；

试 则
    生命周期.资源能力（能力）；
救 所误 则
    置 捕获错误上限 为 真；
终

测试.断言（捕获错误上限，「非整数资源硬上限必须失败」）；

定 一次平台计时器 为 假平台计时器（7）；

定 一次任务 为 生命周期.计时任务（一次平台计时器，10，假，记录移除）；

测试.断言（一次任务.触发（记录触发，{「类型」：「计时器」}），「一次任务应执行回调」）；

测试.相等（一次任务.状态（），「完成」）；

测试.相等（一次平台计时器.关闭数，1）；

测试.相等（回调总数，1）；

测试.相等（移除总数，1）；

测试.断言（非 一次任务.关闭（），「完成任务重复关闭必须幂等」）；

测试.断言（（长度（JSON.序列化（一次任务.快照（））） 大于 0），「计时任务快照不得包含回调或资源」）；

定 失败平台计时器 为 假平台计时器（8）；

定 失败任务 为 生命周期.计时任务（失败平台计时器，20，真，记录移除）；

测试.断言（非 失败任务.触发（失败触发，{「类型」：「计时器」}），「失败回调必须被隔离」）；

测试.相等（失败任务.状态（），「失败」）；

测试.相等（失败平台计时器.关闭数，1）；

定 取消平台计时器 为 假平台计时器（9）；

定 取消任务 为 生命周期.计时任务（取消平台计时器，30，真，记录移除）；

测试.断言（取消任务.取消（），「运行中任务应可取消」）；

测试.相等（取消任务.状态（），「已取消」）；

测试.相等（取消平台计时器.取消数，1）；

测试.相等（取消平台计时器.关闭数，1）；

测试.断言（非 取消任务.取消（），「重复取消必须幂等」）；

定 已关闭任务 为 生命周期.计时任务（已关闭平台计时器（），30，真，记录移除）；

测试.断言（已关闭任务.取消（），「平台已回收的计时器应按幂等取消处理」）；

测试.相等（已关闭任务.状态（），「已取消」）；

定 假图片 为 假平台图片（21，假，假）；

定 图片资源 为 生命周期.图片资源（假图片，1，记录图片移除）；

测试.断言（图片资源.可用（），「新建图片资源应可用」）；

测试.相等（图片资源.查询（）【「宽」】，2）；

测试.相等（图片资源.查询（）【「字节数」】，8）；

测试.断言（（长度（JSON.序列化（图片资源.快照（））） 大于 0），「图片快照不得包含原生句柄」）；

测试.断言（图片资源.关闭（），「图片资源首次关闭应成功」）；

测试.相等（图片资源.状态（），「已关闭」）；

测试.相等（图片资源.句柄，空）；

测试.相等（假图片.关闭数，1）；

测试.相等（图片移除总数，1）；

测试.断言（非 图片资源.关闭（），「图片资源重复关闭必须幂等」）；

定 已关闭图片资源 为 生命周期.图片资源（已关闭平台图片（），4，记录图片移除）；

测试.断言（已关闭图片资源.关闭（），「平台已回收的图片应按幂等关闭处理」）；

测试.相等（已关闭图片资源.状态（），「已关闭」）；

测试.相等（已关闭图片资源.句柄，空）；

定 关闭失败图片 为 假平台图片（22，假，真）；

定 失败图片资源 为 生命周期.图片资源（关闭失败图片，2，记录图片移除）；

测试.断言（失败图片资源.关闭（），「失败关闭也必须完成本地状态迁移」）；

测试.相等（失败图片资源.状态（），「失败」）；

测试.相等（失败图片资源.快照（）【「错误」】【「代码」】，「TEST_IMAGE_CLOSE」）；

测试.相等（图片移除总数，3）；

定 查询失败图片 为 假平台图片（23，真，假）；

令 捕获图片查询失败 为 假；

试 则
    生命周期.图片资源（查询失败图片，3，记录图片移除）；
救 所误 则
    置 捕获图片查询失败 为 真；
终

测试.断言（捕获图片查询失败，「图片查询失败必须回滚平台资源」）；

测试.相等（查询失败图片.关闭数，1）；

测试.相等（图片移除总数，3）；

定 关闭记录 为 生命周期.关闭状态（「测试资源」）；

测试.断言（非 关闭记录.执行（「过早」，成功关闭步骤），「关闭开始前不得执行步骤」）；

测试.断言（关闭记录.开始（），「可用资源应进入关闭中」）；

测试.断言（非 关闭记录.开始（），「关闭状态只能单向开始一次」）；

测试.断言（关闭记录.执行（「第一步」，成功关闭步骤），「成功步骤应返回真」）；

测试.断言（关闭记录.执行（「已关闭步骤」，已关闭步骤），「平台已回收的资源应视为关闭成功」）；

测试.断言（非 关闭记录.执行（「失败步骤」，失败关闭步骤），「失败步骤必须被隔离」）；

测试.断言（关闭记录.执行（「后续步骤」，成功关闭步骤），「失败后仍应继续后续清理」）；

测试.相等（关闭步骤总数，2）；

测试.断言（关闭记录.完成（），「关闭中应单向进入已关闭」）；

测试.断言（非 关闭记录.完成（），「重复完成必须幂等」）；

定 关闭快照 为 关闭记录.快照（）；

测试.相等（关闭快照【「状态」】，「已关闭」）；

测试.断言（非 关闭快照【「成功」】，「任一步骤失败应使关闭报告失败」）；

测试.相等（关闭快照【「步骤总数」】，4）；

测试.相等（关闭快照【「成功总数」】，3）；

测试.相等（关闭快照【「错误总数」】，1）；

测试.相等（关闭快照【「错误」】【0】【「步骤」】，「失败步骤」）；

测试.相等（关闭快照【「错误」】【0】【「错误」】【「代码」】，「TEST_CLOSE_STEP」）；

测试.断言（（长度（JSON.序列化（关闭快照）） 大于 0），「关闭报告必须可安全序列化」）；

置 关闭快照【「错误」】【0】【「错误」】【「代码」】 为 「已修改」；

测试.相等（关闭记录.快照（）【「错误」】【0】【「错误」】【「代码」】，「TEST_CLOSE_STEP」）；

言 「生命周期能力测试通过」；

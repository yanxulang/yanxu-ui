引「../src/数据/源.yx」为 数据；

引「标准:测试」为 测试；

令 更新次数 为 0；

令 失败次数 为 0；

公 类 观察者 则
    公 域 次数：数；
    公 法 初始化（） 则
        置 此.次数 为 0；
    终
    公 法 接收（数据值：典） 则
        置 此.次数 为 （此.次数 加 1）；
    终
终

法 加载项目（起点：数，数量：数）：列 则
    令 结果：列 为 【】；
    逐 偏移 于 范围（0，数量） 则
        追加（结果，（起点 加 偏移））；
    终
    归 结果；
终

法 记录更新（数据值：典） 则
    置 更新次数 为 （更新次数 加 1）；
终

法 记录失败（数据值：典） 则
    置 失败次数 为 （失败次数 加 1）；
终

定 源 为 数据.数据源（100，加载项目）；

源.监听（「数据更新」，记录更新）；

源.监听（「加载失败」，记录失败）；

定 对象观察 为 观察者（）；

源.监听（「数据更新」，对象观察.接收）；

测试.相等（源.数量（），100）；

测试.相等（源.状态（），「空闲」）；

测试.相等（源.版本（），0）；

定 首批 为 源.加载（4，3）；

测试.相等（首批【0】，4）；

测试.相等（首批【2】，6）；

测试.相等（源.取（5），5）；

测试.断言（源.已缓存（6），「加载结果应进入有界缓存」）；

测试.相等（源.状态（），「完成」）；

测试.相等（更新次数，1）；

测试.相等（对象观察.次数，1）；

源.提供（8，【8，9】）；

测试.相等（源.取（8），8）；

测试.相等（源.取（9），9）；

测试.相等（更新次数，2）；

源.失效（4，2）；

测试.断言（非 源.已缓存（4），「失效范围必须移除缓存」）；

测试.断言（源.已缓存（6），「失效范围外的缓存必须保留」）；

源.最大缓存（2）；

源.提供（20，【20，21，22】）；

测试.相等（源.快照（）【「缓存数量」】，2）；

法 空加载（起点：数，数量：数）：列 则
    归 【】；
终

定 错误源 为 数据.数据源（10，空加载）；

错误源.监听（「加载失败」，记录失败）；

定 错误结果 为 错误源.加载（0，2）；

测试.相等（长度（错误结果），0）；

测试.相等（错误源.状态（），「失败」）；

测试.相等（失败次数，1）；

言 「数据源测试通过」；

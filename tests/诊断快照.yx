引「../src/核心/帧调度.yx」为 帧调度；

引「../src/核心/脏区.yx」为 脏区；

引「../src/核心/诊断.yx」为 诊断；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

公 类 假控件 则
    公 域 编号值：数；
    公 法 初始化（编号值：数） 则
        置 此.编号值 为 编号值；
    终
    公 法 编号（）：数 则
        归 此.编号值；
    终
终

公 类 假焦点 则
    公 域 当前：任意；
    公 法 初始化（当前值） 则
        置 此.当前 为 当前值；
    终
    公 法 范围深度（）：数 则
        归 1；
    终
终

公 类 假叠层 则
    公 法 数量（）：数 则
        归 2；
    终
终

公 类 假平台窗口 则
    公 法 查询（）：典 则
        归 {「编号」：7，「标题」：「不得进入诊断」，「宽」：320，「高」：200，「可见」：真，「显示器」：{「名称」：「不得进入诊断」，「位置」：【0，0】，「尺寸」：【1920，1080】，「比例因子」：2，「主显示器」：真}，「待呈现帧」：假，「帧字节」：64}；
    终
终

公 类 假平台应用 则
    公 法 调试快照（）：典 则
        归 {「应用状态」：「就绪」，「帧统计」：{「待呈现」：0}}；
    终
终

公 类 假主题 则
    公 法 名称（）：文 则
        归 「测试主题」；
    终
终

公 类 假窗口 则
    公 域 窗口编号：数；
    公 域 已关闭：理；
    公 域 宽：数；
    公 域 高：数；
    公 域 需要布局状态：理；
    公 域 需要语义状态：理；
    公 域 语义摘要：典；
    公 域 脏区值：任意；
    公 域 焦点管理：任意；
    公 域 捕获控件：任意；
    公 域 悬停控件：任意；
    公 域 叠层管理：任意；
    公 域 帧调度值：任意；
    公 域 平台窗口：任意；
    公 法 初始化（） 则
        置 此.窗口编号 为 7；
        置 此.已关闭 为 假；
        置 此.宽 为 320；
        置 此.高 为 200；
        置 此.需要布局状态 为 真；
        置 此.需要语义状态 为 假；
        置 此.语义摘要 为 {「修订」：3，「节点数」：5，「焦点」：11}；
        置 此.脏区值 为 脏区.脏区管理器（【0，0，320，200】，8）；
        此.脏区值.标记（【10，10，20，20】）；
        置 此.焦点管理 为 假焦点（假控件（11））；
        置 此.捕获控件 为 假控件（12）；
        置 此.悬停控件 为 空；
        置 此.叠层管理 为 假叠层（）；
        置 此.帧调度值 为 帧调度.帧调度器（4）；
        置 此.平台窗口 为 假平台窗口（）；
    终
    公 法 生命周期快照（）：典 则
        归 {「名称」：「窗口」，「状态」：「可用」，「成功」：假，「步骤总数」：0，「成功总数」：0，「错误总数」：0，「错误」：【】}；
    终
终

法 空回调（） 则
终

公 类 假应用 则
    公 域 平台应用：任意；
    公 域 窗口列：列；
    公 域 主题对象：任意；
    公 域 计时回调：典；
    公 域 应用监听：典；
    公 法 初始化（窗口值） 则
        置 此.平台应用 为 假平台应用（）；
        置 此.窗口列 为 【窗口值】；
        置 此.主题对象 为 假主题（）；
        置 此.计时回调 为 {「1」：{「回调」：空回调}，「2」：空}；
        置 此.应用监听 为 {「应用启动」：【空回调】，「退出请求」：【空回调，空回调】}；
    终
    公 法 生命周期快照（）：典 则
        归 {「名称」：「应用」，「状态」：「可用」，「成功」：假，「步骤总数」：0，「成功总数」：0，「错误总数」：0，「错误」：【】}；
    终
    公 法 活动图片资源数（）：数 则
        归 3；
    终
终

定 窗口值 为 假窗口（）；

定 应用值 为 假应用（窗口值）；

定 窗口快照 为 诊断.窗口快照（窗口值）；

测试.相等（窗口快照【「版本」】，2）；

测试.相等（窗口快照【「生命周期」】【「状态」】，「可用」）；

测试.相等（窗口快照【「尺寸」】【0】，320）；

测试.相等（窗口快照【「交互」】【「焦点」】，11）；

测试.相等（窗口快照【「交互」】【「捕获」】，12）；

测试.相等（窗口快照【「交互」】【「叠层数」】，2）；

测试.相等（窗口快照【「无障碍」】【「节点数」】，5）；

测试.相等（窗口快照【「布局」】【「脏区数」】，1）；

测试.断言（窗口快照【「平台可用」】，「活动平台窗口查询必须可用」）；

测试.相等（窗口快照【「平台」】【「编号」】，7）；

测试.相等（窗口快照【「平台」】【「宽」】，320）；

测试.相等（窗口快照【「平台」】【「帧字节」】，64）；

测试.断言（非 包含（键列（窗口快照【「平台」】），「标题」），「窗口诊断不得包含平台标题」）；

测试.断言（非 包含（键列（窗口快照【「平台」】【「显示器」】），「名称」），「窗口诊断不得包含显示器名称」）；

定 应用快照 为 诊断.应用快照（应用值）；

测试.相等（应用快照【「版本」】，2）；

测试.相等（应用快照【「生命周期」】【「状态」】，「可用」）；

测试.相等（应用快照【「窗口数」】，1）；

测试.相等（应用快照【「主题」】，「测试主题」）；

测试.相等（应用快照【「活动计时回调」】，1）；

测试.相等（应用快照【「活动图片资源」】，3）；

测试.相等（应用快照【「监听」】【「类型数」】，2）；

测试.相等（应用快照【「监听」】【「回调数」】，3）；

测试.断言（（长度（JSON.序列化（应用快照）） 大于 0），「诊断快照不得泄漏回调或资源对象」）；

置 应用快照【「窗口」】【0】【「尺寸」】【0】 为 999；

测试.相等（诊断.应用快照（应用值）【「窗口」】【0】【「尺寸」】【0】，320）；

置 窗口值.已关闭 为 真；

定 关闭快照 为 诊断.窗口快照（窗口值）；

测试.断言（非 关闭快照【「平台可用」】，「关闭窗口不应查询已释放平台资源」）；

测试.相等（关闭快照【「平台错误」】，空）；

言 「诊断快照测试通过」；

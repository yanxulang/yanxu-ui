引「包:言界」为 界面；

定 应用 为 界面.应用（「言界原生冒烟」）；

定 窗口 为 应用.窗口（{「标题」：「言界原生冒烟」，「宽」：520，「高」：300，「可见」：假}）；

定 布局 为 窗口.列（{「间距」：12，「内边距」：24}）；

布局.文字（「由言序控件树绘制的原生窗口」）；

定 输入 为 布局.输入框（{「内容」：「中文 IME」，「占位」：「请输入」}）；

定 按钮 为 布局.按钮（「言序按钮」）；

法 点击（所事件） 则
    输入.内容（「按钮已由言序事件路由触发」）；
终

按钮.点击（点击）；

法 自动退出（原始） 则
    应用.退出（）；
终

应用.定时器（800，假，自动退出）；

窗口.显示（）；

应用.运行（）；

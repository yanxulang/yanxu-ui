引「包:言界」为 界面；

引「包:言界/数据源」为 数据；

定 应用 为 界面.应用（「虚拟数据视图」）；

定 窗口 为 应用.窗口（{「标题」：「言界数据视图」，「宽」：640，「高」：480}）；

定 布局 为 窗口.列（{「内边距」：20，「间距」：10}）；

布局.文字（「只加载视口附近的数据；滚动时按范围增量更新」）；

法 加载（起点：数，数量：数）：列 则
    令 结果：列 为 【】；
    逐 序 于 范围（0，数量） 则
        追加（结果，「可见数据行」）；
    终
    归 结果；
终

定 源 为 数据.数据源（10000，加载）；

定 视图 为 布局.虚拟列表（源，{「首选高」：320，「行高」：28，「伸展」：1}）；

法 关闭（所事件） 则
    应用.退出（）；
终

窗口.关闭时（关闭）；

窗口.显示（）；

应用.运行（）；

引「包:言界」为 界面；

定 应用 为 界面.应用（「按钮和布局」）；
定 窗口 为 应用.窗口（{「标题」：「按钮和布局」，「宽」：680，「高」：420}）；
定 主列 为 窗口.列（{「内边距」：24，「间距」：14}）；
定 状态 为 主列.文字（「尚未点击」）；
定 按钮行 为 主列.行（{「间距」：10}）；
定 保存 为 按钮行.按钮（「保存」）；
定 取消 为 按钮行.按钮（「取消」）；
法 保存点击（所事件）则 状态.内容（「已保存」）；终
法 取消点击（所事件）则 状态.内容（「已取消」）；终
保存.点击（保存点击）；取消.点击（取消点击）；
法 关闭处理（所事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；

引「包:言界」为 界面；

定 应用 为 界面.应用（「主题切换」）；
定 窗口 为 应用.窗口（{「标题」：「浅色与深色主题」，「宽」：640，「高」：420}）；
定 布局 为 窗口.列（{「内边距」：24，「间距」：12}）；
布局.文字（「主题由言据配置驱动并在控件树继承」）；
布局.输入框（{「内容」：「主题中的输入框」}）；
定 切换 为 布局.按钮（「切换深色」）；
令 深色 为 假；
法 切换主题（所事件）则
    置 深色 为 非 深色；
    若 深色 则 应用.主题（「深色」）；切换.内容（「切换浅色」）；
    否则 应用.主题（「浅色」）；切换.内容（「切换深色」）；终
终
切换.点击（切换主题）；
法 关闭处理（所事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；

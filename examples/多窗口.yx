引「包:言界」为 界面；

定 应用 为 界面.应用（「多窗口」）；

定 主窗 为 应用.窗口（{「标题」：「主窗口」，「宽」：560，「高」：360}）；

定 副窗 为 应用.窗口（{「标题」：「第二窗口」，「宽」：440，「高」：280}）；

主窗.列（{「内边距」：20}）.文字（「两个窗口共享同一言序应用事件循环」）；

副窗.列（{「内边距」：20}）.输入框（{「内容」：「独立焦点与控件树」}）；

法 关闭处理（所事件） 则
    应用.退出（）；
终

主窗.关闭时（关闭处理）；

副窗.关闭时（关闭处理）；

主窗.显示（）；

副窗.显示（）；

应用.运行（）；

引「包:言界」为 界面；

定 应用 为 界面.应用（「文本输入」）；
定 窗口 为 应用.窗口（{「标题」：「文本输入」，「宽」：720，「高」：460}）；
定 布局 为 窗口.列（{「内边距」：24，「间距」：12}）；
布局.文字（「单行与多行文本编辑」）；
定 单行 为 布局.输入框（{「占位」：「姓名」}）；
定 多行 为 布局.多行输入框（{「占位」：「支持选区、撤销、重做与多行滚动」，「首选高」：180}）；
定 回显 为 布局.文字（「等待输入」）；
法 内容变化（所事件）则 回显.内容（「当前单行内容：」加 单行.取内容（））；终
单行.变化时（内容变化）；
法 关闭处理（所事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；

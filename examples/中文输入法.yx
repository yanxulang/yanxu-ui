引「包:言界」为 界面；

定 应用 为 界面.应用（「中文输入法」）；

定 窗口 为 应用.窗口（{「标题」：「中文 IME 验证」，「宽」：720，「高」：420}）；

定 布局 为 窗口.列（{「内边距」：24，「间距」：12}）；

布局.文字（「请测试拼音／五笔候选、组合更新、提交和取消」）；

定 输入 为 布局.多行输入框（{「内容」：「中文 English 😀 𠀀\n」，「占位」：「在此输入中文」，「首选高」：220}）；

输入.可访问名称（「中文多行输入」）；

法 关闭处理（所事件） 则
    应用.退出（）；
终

窗口.关闭时（关闭处理）；

窗口.显示（）；

应用.运行（）；

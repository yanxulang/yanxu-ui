引「包:言界」为 界面；

定 应用 为 界面.应用（「滚动列表」）；
定 窗口 为 应用.窗口（{「标题」：「滚动与列表」，「宽」：680，「高」：520}）；
定 布局 为 窗口.列（{「内边距」：20，「间距」：12}）；
布局.文字（「滚轮可滚动，下方列表支持选择」）；
定 滚动区 为 布局.滚动（{「首选高」：180，「间距」：4}）；
逐 序 于 范围（0，30）则 滚动区.文字（「滚动内容」）；终
定 列表值 为 布局.列表（【「北京」，「上海」，「广州」，「深圳」，「杭州」，「成都」】）；
列表值.尺寸（320，180）；
法 关闭处理（所事件）则 应用.退出（）；终
窗口.关闭时（关闭处理）；窗口.显示（）；应用.运行（）；

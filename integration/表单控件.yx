引「../src/主.yx」为 界面；

引「../src/事件/路由.yx」为 事件；

引「../src/渲染/命令.yx」为 渲染；

引「标准:测试」为 测试；

引「标准:数学」为 数学；

定 根 为 界面.容器（空，「列」，{「间距」：8}）；

定 复选 为 根.复选框（「接受条款」）；

根.布局（【0，0，320，80】）；

测试.断言（非 复选.取已检查（），「复选框默认不选中」）；

定 初始语义 为 复选.语义（）.快照（）；

测试.相等（初始语义【「角色」】，「复选框」）；

测试.断言（非 初始语义【「状态」】【「已检查」】，「语义树应报告未选中」）；

测试.断言（包含（初始语义【「操作」】，「点击」），「可编辑复选框应声明点击」）；

令 变化次数 为 0；

令 最后检查 为 假；

法 记录变化（所事件） 则
    置 变化次数 为 （变化次数 加 1）；
    置 最后检查 为 所事件.字段（「已检查」，假）；
终

复选.变化时（记录变化）；

定 路由 为 事件.路由器（）；

路由.发送（复选，{「类型」：「指针按下」，「横坐标」：（复选.框【0】 加 4），「纵坐标」：（复选.框【1】 加 4）}）；

路由.发送（复选，{「类型」：「指针释放」，「横坐标」：（复选.框【0】 加 4），「纵坐标」：（复选.框【1】 加 4）}）；

测试.断言（复选.取已检查（），「指针释放应选中复选框」）；

测试.相等（变化次数，1）；

测试.断言（最后检查，「变化事件应携带新检查状态」）；

路由.发送（复选，{「类型」：「按键按下」，「逻辑键」：「 」}）；

路由.发送（复选，{「类型」：「按键释放」，「逻辑键」：「 」}）；

测试.断言（非 复选.取已检查（），「空格键应切换复选框」）；

测试.相等（变化次数，2）；

复选.混合（真）；

定 混合语义 为 复选.语义（）.快照（）；

测试.断言（混合语义【「状态」】【「混合」】，「复选框应公开混合态」）；

测试.断言（非 混合语义【「状态」】【「已检查」】，「混合态不得同时标记已检查」）；

测试.断言（复选.执行无障碍动作（「点击」，空），「辅助技术点击应切换复选框」）；

测试.断言（复选.取已检查（），「混合态点击后应变为选中」）；

复选.必填（真）；

复选.无效（真）；

复选.只读（真）；

定 只读语义 为 复选.语义（）.快照（）；

测试.断言（只读语义【「状态」】【「只读」】，「复选框应公开只读状态」）；

测试.断言（只读语义【「状态」】【「必填」】，「复选框应公开必填状态」）；

测试.断言（只读语义【「状态」】【「无效」】，「复选框应公开无效状态」）；

测试.断言（非 包含（只读语义【「操作」】，「点击」），「只读复选框不得声明点击」）；

测试.断言（非 复选.执行无障碍动作（「点击」，空），「只读复选框应拒绝辅助技术点击」）；

测试.断言（复选.取已检查（），「拒绝的点击不得改变状态」）；

定 树 为 根.渲染树（）；

定 帧 为 渲染.生成帧（树，【0，0，320，80】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（帧.编码（））【「操作码」】） 大于 6），「复选框应生成完整绘制命令」）；

定 单选根 为 界面.容器（空，「列」，{「间距」：6}）；

定 无效组根 为 界面.容器（空，「列」，{}）；

令 捕获无效组 为 假；

试 则
    界面.单选框（无效组根，「单选框」，{「内容」：「无效」，「组」：「」}）；
救 所误 则
    置 捕获无效组 为 真；
终

测试.断言（捕获无效组，「空单选组必须被拒绝」）；

测试.相等（长度（无效组根.子列），0）；

定 单选甲 为 单选根.单选框（「甲」，「方案」）；

定 单选乙 为 单选根.单选框（「乙」，「方案」）；

定 单选丙 为 单选根.单选框（「丙」，「方案」）；

单选根.布局（【0，0，320，120】）；

单选甲.选中（真）；

测试.断言（单选甲.取选中（），「程序设置应选中单选项」）；

测试.断言（非 单选乙.取选中（），「同组其他项目应保持未选中」）；

令 可聚焦项：列 为 【】；

单选根.收集可聚焦（可聚焦项）；

测试.相等（长度（可聚焦项），1）；

测试.相等（可聚焦项【0】.编号（），单选甲.编号（））；

路由.发送（单选乙，{「类型」：「指针按下」，「横坐标」：（单选乙.框【0】 加 4），「纵坐标」：（单选乙.框【1】 加 4）}）；

路由.发送（单选乙，{「类型」：「指针释放」，「横坐标」：（单选乙.框【0】 加 4），「纵坐标」：（单选乙.框【1】 加 4）}）；

测试.断言（单选乙.取选中（），「指针应选择第二个单选项」）；

测试.断言（非 单选甲.取选中（），「选择新项应取消同组旧项」）；

路由.发送（单选乙，{「类型」：「按键按下」，「逻辑键」：「ArrowRight」}）；

测试.断言（单选丙.取选中（），「向右键应循环到下一个同组项目」）；

路由.发送（单选丙，{「类型」：「按键按下」，「逻辑键」：「ArrowRight」}）；

测试.断言（单选甲.取选中（），「方向键导航应在组尾循环」）；

定 单选语义 为 单选甲.语义（）.快照（）；

测试.相等（单选语义【「角色」】，「单选框」）；

测试.断言（单选语义【「状态」】【「选中」】，「单选框应公开选中状态」）；

测试.断言（包含（单选语义【「操作」】，「选择」），「可编辑单选框应声明选择操作」）；

单选乙.只读（真）；

测试.断言（非 单选乙.执行无障碍动作（「选择」，空），「只读单选框应拒绝辅助技术选择」）；

测试.断言（单选甲.执行无障碍动作（「选择」，空），「已选项重复选择应幂等成功」）；

定 单选树 为 单选根.渲染树（）；

定 单选帧 为 渲染.生成帧（单选树，【0，0，320，120】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（单选帧.编码（））【「操作码」】） 大于 10），「单选框应生成完整绘制命令」）；

定 切换根 为 界面.容器（空，「列」，{}）；

定 所切换 为 切换根.切换（「自动保存」）；

切换根.布局（【0，0，320，60】）；

测试.断言（非 所切换.取开启（），「切换控件默认关闭」）；

路由.发送（所切换，{「类型」：「指针按下」，「横坐标」：（所切换.框【0】 加 4），「纵坐标」：（所切换.框【1】 加 4）}）；

路由.发送（所切换，{「类型」：「指针释放」，「横坐标」：（所切换.框【0】 加 4），「纵坐标」：（所切换.框【1】 加 4）}）；

测试.断言（所切换.取开启（），「指针应开启切换控件」）；

路由.发送（所切换，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

定 按下语义 为 所切换.语义（）.快照（）；

测试.断言（按下语义【「状态」】【「按下」】，「键盘按下期间应公开按下状态」）；

路由.发送（所切换，{「类型」：「按键释放」，「逻辑键」：「Enter」}）；

测试.断言（非 所切换.取开启（），「回车键应关闭切换控件」）；

测试.断言（所切换.执行无障碍动作（「点击」，空），「辅助技术点击应开启切换控件」）；

定 切换语义 为 所切换.语义（）.快照（）；

测试.相等（切换语义【「角色」】，「切换」）；

测试.断言（切换语义【「状态」】【「已检查」】，「切换控件应公开检查状态」）；

所切换.必填（真）；

所切换.无效（真）；

所切换.只读（真）；

定 只读切换语义 为 所切换.语义（）.快照（）；

测试.断言（只读切换语义【「状态」】【「只读」】，「切换控件应公开只读状态」）；

测试.断言（只读切换语义【「状态」】【「必填」】，「切换控件应公开必填状态」）；

测试.断言（只读切换语义【「状态」】【「无效」】，「切换控件应公开无效状态」）；

测试.断言（非 包含（只读切换语义【「操作」】，「点击」），「只读切换控件不得声明点击」）；

测试.断言（非 所切换.执行无障碍动作（「点击」，空），「只读切换控件应拒绝辅助技术点击」）；

定 切换树 为 切换根.渲染树（）；

定 切换帧 为 渲染.生成帧（切换树，【0，0，320，60】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（切换帧.编码（））【「操作码」】） 大于 7），「切换控件应生成完整绘制命令」）；

定 无效下拉根 为 界面.容器（空，「列」，{}）；

令 捕获非列选项 为 假；

试 则
    界面.下拉选择（无效下拉根，「下拉选择」，{「选项」：「甲」}）；
救 所误 则
    置 捕获非列选项 为 真；
终

测试.断言（捕获非列选项，「下拉选项必须是文字列」）；

令 捕获非文选项 为 假；

试 则
    界面.下拉选择（无效下拉根，「下拉选择」，{「选项」：【「甲」，1】}）；
救 所误 则
    置 捕获非文选项 为 真；
终

测试.断言（捕获非文选项，「下拉选项必须全部是文字」）；

令 捕获过多选项 为 假；

试 则
    界面.下拉选择（无效下拉根，「下拉选择」，{「选项」：范围（0，4097）}）；
救 所误 则
    置 捕获过多选项 为 真；
终

测试.断言（捕获过多选项，「下拉选项必须有明确数量上限」）；

令 过长选项 为 「界」；

逐 次 于 范围（0，15） 则
    置 过长选项 为 （过长选项 加 过长选项）；
终

令 捕获过长选项 为 假；

试 则
    界面.下拉选择（无效下拉根，「下拉选择」，{「选项」：【过长选项】}）；
救 所误 则
    置 捕获过长选项 为 真；
终

测试.断言（捕获过长选项，「单个下拉选项必须遵守文字字节上限」）；

测试.相等（长度（无效下拉根.子列），0）；

定 下拉根 为 界面.容器（空，「列」，{}）；

定 所下拉 为 下拉根.下拉选择（【「红」，「绿」，「蓝」，「黄」，「紫」】）；

下拉根.布局（【0，0，320，60】）；

测试.相等（所下拉.取选择（），减 1）；

测试.相等（所下拉.取值（），空）；

测试.断言（所下拉.选择（1），「程序应能选择有效下拉项目」）；

测试.相等（所下拉.取值（），「绿」）；

测试.断言（非 所下拉.选择（5），「越界程序选择必须被拒绝」）；

测试.相等（所下拉.取选择（），1）；

令 选项副本 为 所下拉.取选项（）；

追加（选项副本，「外部修改」）；

测试.相等（长度（所下拉.取选项（）），5）；

令 下拉变化次数 为 0；

令 最后下拉序 为 减 1；

法 记录下拉变化（所事件） 则
    置 下拉变化次数 为 （下拉变化次数 加 1）；
    置 最后下拉序 为 所事件.字段（「选择」，减 1）；
终

所下拉.变化时（记录下拉变化）；

路由.发送（所下拉，{「类型」：「指针按下」，「横坐标」：（所下拉.框【0】 加 4），「纵坐标」：（所下拉.框【1】 加 4）}）；

路由.发送（所下拉，{「类型」：「指针释放」，「横坐标」：（所下拉.框【0】 加 4），「纵坐标」：（所下拉.框【1】 加 4）}）；

测试.断言（所下拉.取展开（），「点击下拉主体应展开弹框」）；

定 下拉弹框 为 所下拉.命中（（所下拉.框【0】 加 4），（（所下拉.框【1】 加 所下拉.框【3】） 加 4））；

测试.相等（下拉弹框.编号（），所下拉.编号（））；

定 蓝项纵坐标 为 （（（所下拉.框【1】 加 所下拉.框【3】） 加 （2 乘 所下拉.选项高）） 加 4）；

路由.发送（所下拉，{「类型」：「指针按下」，「横坐标」：（所下拉.框【0】 加 4），「纵坐标」：蓝项纵坐标}）；

路由.发送（所下拉，{「类型」：「指针释放」，「横坐标」：（所下拉.框【0】 加 4），「纵坐标」：蓝项纵坐标}）；

测试.相等（所下拉.取选择（），2）；

测试.相等（最后下拉序，2）；

测试.相等（下拉变化次数，1）；

测试.断言（非 所下拉.取展开（），「指针选择后应折叠弹框」）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「ArrowDown」}）；

测试.断言（所下拉.取展开（），「方向键应展开下拉选择」）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「ArrowDown」}）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

测试.相等（所下拉.取选择（），3）；

测试.相等（下拉变化次数，2）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

测试.断言（所下拉.取展开（），「回车应展开已折叠的下拉选择」）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Escape」}）；

测试.断言（非 所下拉.取展开（），「Escape 应折叠下拉选择」）；

测试.相等（所下拉.取选择（），3）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「End」}）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

测试.相等（所下拉.取选择（），4）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Home」}）；

路由.发送（所下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

测试.相等（所下拉.取选择（），0）；

定 长下拉根 为 界面.容器（空，「列」，{}）；

定 长下拉 为 界面.下拉选择（长下拉根，「下拉选择」，{「选项」：【「红」，「橙」，「黄」，「绿」，「青」，「蓝」，「紫」】，「最大可见」：3}）；

长下拉根.布局（【0，0，320，60】）；

测试.断言（长下拉.展开（），「有项目的下拉选择应允许展开」）；

路由.发送（长下拉，{「类型」：「滚轮」，「横滚」：0，「纵滚」：1}）；

测试.相等（长下拉.首可见序，1）；

路由.发送（长下拉，{「类型」：「文本输入」，「文本」：「蓝」}）；

测试.相等（长下拉.悬停序，5）；

测试.断言（（长下拉.首可见序 不小于 3），「首字匹配应滚动到可见项目」）；

路由.发送（长下拉，{「类型」：「按键按下」，「逻辑键」：「Enter」}）；

测试.相等（长下拉.取值（），「蓝」）；

路由.发送（长下拉，{「类型」：「文本输入」，「文本」：「紫」}）；

测试.相等（长下拉.取值（），「紫」）；

测试.断言（长下拉.执行无障碍动作（「设置值」，2），「辅助技术应能用整数索引设置下拉值」）；

测试.相等（长下拉.取值（），「黄」）；

测试.断言（长下拉.执行无障碍动作（「设置值」，「绿」），「辅助技术应能用精确文字设置下拉值」）；

测试.相等（长下拉.取选择（），3）；

测试.断言（非 长下拉.执行无障碍动作（「设置值」，2.5），「辅助技术不得使用非整数索引」）；

测试.断言（非 长下拉.执行无障碍动作（「设置值」，「不存在」），「辅助技术不得设置未知选项」）；

测试.断言（长下拉.执行无障碍动作（「展开」，空），「辅助技术应能展开组合框」）；

测试.断言（长下拉.执行无障碍动作（「折叠」，空），「辅助技术应能折叠组合框」）；

长下拉.选择（3）；

长下拉.展开（）；

长下拉.选项（【「黑」，「绿」，「白」】）；

测试.断言（非 长下拉.取展开（），「程序替换选项应折叠弹框并释放交互状态」）；

测试.相等（长下拉.取选择（），1）；

测试.相等（长下拉.取值（），「绿」）；

长下拉.必填（真）；

长下拉.无效（真）；

定 下拉语义 为 长下拉.语义（）.快照（）；

测试.相等（下拉语义【「角色」】，「组合框」）；

测试.相等（下拉语义【「值」】，「绿」）；

测试.断言（下拉语义【「状态」】【「必填」】，「组合框应公开必填状态」）；

测试.断言（下拉语义【「状态」】【「无效」】，「组合框应公开无效状态」）；

测试.断言（包含（下拉语义【「操作」】，「设置值」），「可编辑组合框应声明设置值」）；

测试.断言（包含（下拉语义【「操作」】，「展开」），「折叠组合框应声明展开」）；

长下拉.展开（）；

长下拉.只读（真）；

测试.断言（非 长下拉.取展开（），「切换为只读应立即折叠下拉选择」）；

定 只读下拉语义 为 长下拉.语义（）.快照（）；

测试.断言（只读下拉语义【「状态」】【「只读」】，「组合框应公开只读状态」）；

测试.断言（非 包含（只读下拉语义【「操作」】，「设置值」），「只读组合框不得声明设置值」）；

测试.断言（非 长下拉.执行无障碍动作（「设置值」，0），「只读组合框应拒绝辅助技术改值」）；

长下拉.只读（假）；

长下拉.展开（）；

定 下拉树 为 长下拉根.渲染树（）；

定 下拉帧 为 渲染.生成帧（下拉树，【0，0，320，180】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（下拉帧.编码（））【「操作码」】） 大于 12），「下拉选择应生成主体和弹框绘制命令」）；

定 无效滑块根 为 界面.容器（空，「列」，{}）；

令 捕获滑块非数 为 假；

试 则
    界面.滑块（无效滑块根，「滑块」，{「最小」：「零」}）；
救 所误 则
    置 捕获滑块非数 为 真；
终

测试.断言（捕获滑块非数，「滑块范围必须使用有限数」）；

令 捕获滑块顺序 为 假；

试 则
    界面.滑块（无效滑块根，「滑块」，{「最小」：10，「最大」：10}）；
救 所误 则
    置 捕获滑块顺序 为 真；
终

测试.断言（捕获滑块顺序，「滑块最大值必须大于最小值」）；

令 捕获滑块步长 为 假；

试 则
    界面.滑块（无效滑块根，「滑块」，{「步长」：0}）；
救 所误 则
    置 捕获滑块步长 为 真；
终

测试.断言（捕获滑块步长，「滑块步长必须大于零」）；

令 捕获滑块方向 为 假；

试 则
    界面.滑块（无效滑块根，「滑块」，{「方向」：「斜向」}）；
救 所误 则
    置 捕获滑块方向 为 真；
终

测试.断言（捕获滑块方向，「滑块方向必须属于公开协议」）；

令 捕获滑块无穷 为 假；

试 则
    界面.滑块（无效滑块根，「滑块」，{「值」：数学.幂（10，400）}）；
救 所误 则
    置 捕获滑块无穷 为 真；
终

测试.断言（捕获滑块无穷，「滑块必须拒绝非有限值」）；

令 捕获滑块跨度 为 假；

定 滑块巨值 为 数学.幂（10，308）；

试 则
    界面.滑块（无效滑块根，「滑块」，{「最小」：（0 减 滑块巨值），「最大」：滑块巨值}）；
救 所误 则
    置 捕获滑块跨度 为 真；
终

测试.断言（捕获滑块跨度，「滑块必须拒绝溢出的范围跨度」）；

测试.相等（长度（无效滑块根.子列），0）；

定 滑块根 为 界面.容器（空，「列」，{}）；

定 所滑块 为 滑块根.滑块（{「最小」：0，「最大」：100，「步长」：10，「值」：23，「可访问名称」：「音量」}）；

滑块根.布局（【0，0，320，60】）；

测试.相等（所滑块.取值（），20）；

测试.相等（所滑块.取最小（），0）；

测试.相等（所滑块.取最大（），100）；

测试.相等（所滑块.取步长（），10）；

测试.相等（所滑块.取方向（），「横向」）；

测试.断言（所滑块.值（26），「程序设置应接受有限滑块值」）；

测试.相等（所滑块.取值（），30）；

测试.断言（非 所滑块.值（数学.幂（10，400）），「程序设置必须拒绝非有限滑块值」）；

所滑块.范围（减 50，50）；

测试.相等（所滑块.取值（），30）；

令 捕获程序范围 为 假；

试 则
    所滑块.范围（5，5）；
救 所误 则
    置 捕获程序范围 为 真；
终

测试.断言（捕获程序范围，「程序设置无效范围必须失败」）；

测试.相等（所滑块.取最小（），减 50）；

测试.相等（所滑块.取最大（），50）；

所滑块.范围（0，100）；

所滑块.步长（10）；

所滑块.值（0）；

令 滑块变化次数 为 0；

令 最后滑块值 为 0；

法 记录滑块变化（所事件） 则
    置 滑块变化次数 为 （滑块变化次数 加 1）；
    置 最后滑块值 为 所事件.字段（「值」，0）；
终

所滑块.变化时（记录滑块变化）；

定 七成横坐标 为 （（所滑块.框【0】 加 8） 加 （（所滑块.框【2】 减 16） 乘 0.7））；

定 滑块中纵坐标 为 （所滑块.框【1】 加 （所滑块.框【3】 除 2））；

路由.发送（所滑块，{「类型」：「指针按下」，「横坐标」：七成横坐标，「纵坐标」：滑块中纵坐标}）；

测试.断言（所滑块.拖动中，「指针按下应开始滑块拖动」）；

测试.相等（所滑块.取值（），70）；

路由.发送（所滑块，{「类型」：「指针移动」，「横坐标」：（（所滑块.框【0】 加 所滑块.框【2】） 加 100），「纵坐标」：滑块中纵坐标}）；

测试.相等（所滑块.取值（），100）；

路由.发送（所滑块，{「类型」：「指针取消」}）；

测试.断言（非 所滑块.拖动中，「指针取消应结束滑块拖动」）；

测试.相等（最后滑块值，100）；

测试.断言（（滑块变化次数 不小于 2），「拖动滑块应发送有界变化事件」）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「Home」}）；

测试.相等（所滑块.取值（），0）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「ArrowRight」}）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「ArrowUp」}）；

测试.相等（所滑块.取值（），20）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「PageUp」}）；

测试.相等（所滑块.取值（），100）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「PageDown」}）；

测试.相等（所滑块.取值（），0）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「End」}）；

路由.发送（所滑块，{「类型」：「按键按下」，「逻辑键」：「ArrowLeft」}）；

测试.相等（所滑块.取值（），90）；

测试.断言（所滑块.执行无障碍动作（「设置值」，45），「辅助技术应能设置滑块有限值」）；

测试.相等（所滑块.取值（），50）；

测试.断言（所滑块.执行无障碍动作（「增加」，空），「辅助技术应能增加滑块值」）；

测试.相等（所滑块.取值（），60）；

测试.断言（所滑块.执行无障碍动作（「减少」，空），「辅助技术应能减少滑块值」）；

测试.相等（所滑块.取值（），50）；

测试.断言（非 所滑块.执行无障碍动作（「设置值」，「五十」），「辅助技术不得用非数设置滑块」）；

所滑块.必填（真）；

所滑块.无效（真）；

定 滑块语义 为 所滑块.语义（）.快照（）；

测试.相等（滑块语义【「角色」】，「滑块」）；

测试.相等（滑块语义【「值」】，50）；

测试.相等（滑块语义【「状态」】【「方向」】，「横向」）；

测试.断言（滑块语义【「状态」】【「必填」】，「滑块应公开必填状态」）；

测试.断言（滑块语义【「状态」】【「无效」】，「滑块应公开无效状态」）；

测试.断言（包含（滑块语义【「操作」】，「设置值」），「可编辑滑块应声明设置值」）；

测试.断言（包含（滑块语义【「操作」】，「增加」），「可编辑滑块应声明增加」）；

测试.断言（包含（滑块语义【「操作」】，「减少」），「可编辑滑块应声明减少」）；

路由.发送（所滑块，{「类型」：「指针按下」，「横坐标」：七成横坐标，「纵坐标」：滑块中纵坐标}）；

所滑块.只读（真）；

测试.断言（非 所滑块.拖动中，「切换为只读应取消滑块拖动」）；

定 只读滑块语义 为 所滑块.语义（）.快照（）；

测试.断言（只读滑块语义【「状态」】【「只读」】，「滑块应公开只读状态」）；

测试.断言（非 包含（只读滑块语义【「操作」】，「设置值」），「只读滑块不得声明设置值」）；

测试.断言（非 所滑块.执行无障碍动作（「增加」，空），「只读滑块应拒绝辅助技术调整」）；

测试.断言（所滑块.值（20），「只读滑块仍应允许程序主动更新」）；

所滑块.只读（假）；

定 纵滑块根 为 界面.容器（空，「列」，{}）；

定 纵滑块 为 纵滑块根.滑块（{「最小」：0，「最大」：10，「步长」：1，「值」：5，「方向」：「纵向」}）；

纵滑块根.布局（【0，0，60，200】）；

定 纵滑块横坐标 为 （纵滑块.框【0】 加 （纵滑块.框【2】 除 2））；

路由.发送（纵滑块，{「类型」：「指针按下」，「横坐标」：纵滑块横坐标，「纵坐标」：（纵滑块.框【1】 加 8）}）；

测试.相等（纵滑块.取值（），10）；

路由.发送（纵滑块，{「类型」：「指针移动」，「横坐标」：纵滑块横坐标，「纵坐标」：（（纵滑块.框【1】 加 纵滑块.框【3】） 减 8）}）；

路由.发送（纵滑块，{「类型」：「指针释放」，「横坐标」：纵滑块横坐标，「纵坐标」：（（纵滑块.框【1】 加 纵滑块.框【3】） 减 8）}）；

测试.相等（纵滑块.取值（），0）；

测试.相等（纵滑块.语义（）.快照（）【「状态」】【「方向」】，「纵向」）；

纵滑块.方向（「横向」）；

测试.相等（纵滑块.取方向（），「横向」）；

定 滑块树 为 滑块根.渲染树（）；

定 滑块帧 为 渲染.生成帧（滑块树，【0，0，320，60】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（滑块帧.编码（））【「操作码」】） 大于 6），「滑块应生成轨道、进度和旋钮绘制命令」）；

定 无效进度根 为 界面.容器（空，「列」，{}）；

令 捕获进度非数 为 假；

试 则
    界面.进度条（无效进度根，「进度条」，{「最大」：「一百」}）；
救 所误 则
    置 捕获进度非数 为 真；
终

测试.断言（捕获进度非数，「进度条范围必须使用有限数」）；

令 捕获进度顺序 为 假；

试 则
    界面.进度条（无效进度根，「进度条」，{「最小」：5，「最大」：4}）；
救 所误 则
    置 捕获进度顺序 为 真；
终

测试.断言（捕获进度顺序，「进度条最大值必须大于最小值」）；

令 捕获进度跨度 为 假；

试 则
    界面.进度条（无效进度根，「进度条」，{「最小」：（0 减 滑块巨值），「最大」：滑块巨值}）；
救 所误 则
    置 捕获进度跨度 为 真；
终

测试.断言（捕获进度跨度，「进度条必须拒绝溢出的范围跨度」）；

令 捕获进度无穷 为 假；

试 则
    界面.进度条（无效进度根，「进度条」，{「值」：数学.幂（10，400）}）；
救 所误 则
    置 捕获进度无穷 为 真；
终

测试.断言（捕获进度无穷，「进度条必须拒绝非有限值」）；

测试.相等（长度（无效进度根.子列），0）；

定 进度根 为 界面.容器（空，「列」，{}）；

定 进度 为 进度根.进度条（{「最小」：0，「最大」：200，「值」：250，「可聚焦」：真，「可访问名称」：「下载进度」}）；

进度根.布局（【0，0，320，40】）；

测试.相等（进度.取最小（），0）；

测试.相等（进度.取最大（），200）；

测试.相等（进度.取值（），200）；

测试.断言（非 进度.允许焦点（），「进度条即使收到配置也不得进入焦点序列」）；

进度.可聚焦（真）；

测试.断言（非 进度.允许焦点（），「进度条不得被程序改成交互控件」）；

测试.断言（进度.值（75），「程序应能设置有限进度值」）；

测试.相等（进度.取值（），75）；

测试.断言（进度.值（减 10），「程序进度值应夹取到范围」）；

测试.相等（进度.取值（），0）；

测试.断言（非 进度.值（数学.幂（10，400）），「程序不得设置非有限进度值」）；

进度.值（75）；

进度.范围（50，60）；

测试.相等（进度.取值（），60）；

令 捕获程序进度范围 为 假；

试 则
    进度.范围（2，2）；
救 所误 则
    置 捕获程序进度范围 为 真；
终

测试.断言（捕获程序进度范围，「无效程序进度范围必须失败」）；

测试.相等（进度.取最小（），50）；

测试.相等（进度.取最大（），60）；

进度.只读（真）；

进度.必填（真）；

进度.无效（真）；

定 确定进度语义 为 进度.语义（）.快照（）；

测试.相等（确定进度语义【「角色」】，「进度条」）；

测试.相等（确定进度语义【「值」】，60）；

测试.相等（长度（确定进度语义【「操作」】），0）；

测试.断言（非 包含（键列（确定进度语义【「状态」】），「只读」），「进度条不得声明协议不允许的只读状态」）；

测试.断言（非 包含（键列（确定进度语义【「状态」】），「必填」），「进度条不得声明协议不允许的必填状态」）；

测试.断言（非 包含（键列（确定进度语义【「状态」】），「无效」），「进度条不得声明协议不允许的无效状态」）；

进度.不定（真）；

测试.断言（进度.取不定（），「进度条应支持不定状态」）；

定 不定进度语义 为 进度.语义（）.快照（）；

测试.相等（不定进度语义【「值」】，空）；

测试.断言（不定进度语义【「状态」】【「忙碌」】，「不定进度应公开忙碌状态」）；

测试.断言（进度.值（55），「不定进度仍应保存程序提供的确定值」）；

进度.不定（假）；

测试.相等（进度.语义（）.快照（）【「值」】，55）；

定 进度树 为 进度根.渲染树（）；

定 进度帧 为 渲染.生成帧（进度树，【0，0，320，40】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（进度帧.编码（））【「操作码」】） 大于 3），「进度条应生成轨道和确定进度绘制命令」）；

进度.不定（真）；

定 不定进度帧 为 渲染.生成帧（进度根.渲染树（），【0，0，320，40】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（不定进度帧.编码（））【「操作码」】） 大于 3），「不定进度条应生成有界活动段」）；

言 「表单控件测试通过」；

引「../src/主.yx」为 界面；

引「../src/数据/源.yx」为 数据；

引「../src/事件/路由.yx」为 事件；

引「../src/渲染/命令.yx」为 渲染；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

法 加载（起点：数，数量：数）：列 则
    令 结果：列 为 【】；
    逐 偏移 于 范围（0，数量） 则
        追加（结果，「数据行」）；
    终
    归 结果；
终

定 源 为 数据.数据源（10000，加载）；

定 根 为 界面.容器（空，「列」，{「间距」：8，「内边距」：8}）；

定 列表值 为 根.虚拟列表（源，{「首选宽」：260，「首选高」：100，「行高」：24}）；

根.布局（【0，0，320，140】）；

测试.相等（列表值.数据数量（），10000）；

测试.相等（列表值.数据状态（），「完成」）；

测试.相等（列表值.行内容（0），「数据行」）；

测试.相等（列表值.行内容（4），「数据行」）；

测试.断言（（列表值.取数据源（） 等于 源），「虚拟列表应保留数据源身份」）；

定 路由 为 事件.路由器（）；

路由.发送（列表值，{「类型」：「滚轮」，「横滚」：0，「纵滚」：72}）；

测试.断言（（列表值.首可见序 不小于 2），「滚轮应推进虚拟窗口」）；

测试.相等（列表值.行内容（列表值.首可见序），「数据行」）；

源.提供（2，【「更新行」，「更新行」】）；

测试.相等（列表值.行内容（2），「更新行」）；

定 树 为 根.渲染树（）；

定 帧 为 渲染.生成帧（树，【0，0，320，140】，【】，【246，248，252，255】）；

测试.断言（（长度（渲染.检查缓冲（帧.编码（））【「操作码」】） 大于 5），「虚拟列表应生成有界可绘制帧」）；

定 语义 为 根.语义（）.快照（）；

定 列表语义 为 语义【「子」】【0】；

测试.相等（列表语义【「值」】【「数量」】，10000）；

测试.相等（列表语义【「值」】【「加载状态」】，「完成」）；

言 「数据视图集成测试通过」；

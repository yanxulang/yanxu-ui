引「../src/主.yx」为 界面；

定 应用 为 界面.应用（「言界真实窗口测试」）；
定 窗口 为 应用.窗口（{「标题」：「言界真实窗口测试」，「宽」：480，「高」：260，「可见」：假}）；
定 布局 为 窗口.列（{「间距」：10，「内边距」：20}）；
布局.文字（「言序控件树 → 言台 → 原生窗口」）；
布局.输入框（{「内容」：「中文 IME」}）；
布局.按钮（「真正的言序按钮」）；
法 自动退出（原始）则 应用.退出（）；终
应用.定时器（600，假，自动退出）；
窗口.显示（）；
应用.运行（）；

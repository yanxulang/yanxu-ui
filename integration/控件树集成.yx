引「../src/主.yx」为 界面；

引「../src/事件/路由.yx」为 事件；

引「../src/渲染/命令.yx」为 渲染；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

定 根 为 界面.容器（空，「列」，{「间距」：8，「内边距」：10}）；

定 标题 为 根.文字（「言界控件树」）；

定 按钮 为 根.按钮（「保存」）；

定 输入 为 根.输入框（{「占位」：「请输入」}）；

按钮.伸展（0）；

输入.伸展（0）；

根.布局（【0，0，320，220】）；

测试.相等（长度（根.子列），3）；

测试.断言（（标题.框【3】 大于 0），「文字应完成测量布局」）；

测试.断言（（按钮.框【1】 大于 标题.框【1】），「列布局应按视觉顺序排列」）；

测试.断言（（根.命中（（按钮.框【0】 加 2），（按钮.框【1】 加 2））.编号（） 等于 按钮.编号（）），「命中测试应返回按钮」）；

令 点击次数 为 0；

法 点击处理（所事件） 则
    置 点击次数 为 （点击次数 加 1）；
终

按钮.点击（点击处理）；

定 路由 为 事件.路由器（）；

路由.发送（按钮，{「类型」：「指针按下」，「横坐标」：（按钮.框【0】 加 2），「纵坐标」：（按钮.框【1】 加 2）}）；

路由.发送（按钮，{「类型」：「指针释放」，「横坐标」：（按钮.框【0】 加 2），「纵坐标」：（按钮.框【1】 加 2）}）；

测试.相等（点击次数，1）；

路由.发送（输入，{「类型」：「文本输入」，「文本」：「中文😀」}）；

测试.相等（输入.取内容（），「中文😀」）；

路由.发送（输入，{「类型」：「按键按下」，「逻辑键」：「ArrowLeft」，「修饰键」：{「Shift」：真}}）；

测试.断言（输入.文档值.有选区（），「Shift+方向键应扩展选区」）；

路由.发送（输入，{「类型」：「IME组合开始」}）；

路由.发送（输入，{「类型」：「IME组合更新」，「文本」：「yanxu」，「选区起」：0，「选区终」：5}）；

测试.断言（输入.文档值.组合中，「输入框应协调 IME 组合态」）；

路由.发送（输入，{「类型」：「IME组合提交」，「文本」：「言序」}）；

测试.断言（非 输入.文档值.组合中，「IME 提交后应结束组合态」）；

定 树 为 根.渲染树（）；

定 帧 为 渲染.生成帧（树，【0，0，320，220】，【】，【246，248，252，255】）；

定 缓冲：字节串 为 帧.编码（）；

测试.断言（（字节.长度（缓冲） 大于 16），「控件树应生成正式二进制绘制帧」）；

测试.断言（（长度（渲染.检查缓冲（缓冲）【「操作码」】） 大于 5），「帧应含控件绘制命令」）；

定 语义树 为 根.语义（）.快照（）；

测试.相等（语义树【「编号」】，根.编号（））；

测试.相等（语义树【「子」】【1】【「编号」】，按钮.编号（））；

测试.相等（语义树【「子」】【1】【「角色」】，「按钮」）；

测试.相等（语义树【「子」】【2】【「角色」】，「输入框」）；

测试.断言（语义树【「子」】【1】【「状态」】【「可聚焦」】，「按钮快照应声明实际焦点能力」）；

测试.相等（语义树【「子」】【1】【「操作」】【0】，「聚焦」）；

测试.相等（语义树【「子」】【1】【「操作」】【1】，「点击」）；

按钮.关闭（）；

测试.相等（长度（根.子列），2）；

测试.断言（按钮.已关闭，「关闭控件应释放树关系」）；

定 表单根 为 界面.容器（空，「列」，{}）；

定 只读输入 为 表单根.输入框（{「内容」：「初始」，「只读」：真，「必填」：真，「无效」：真}）；

表单根.布局（【0，0，320，80】）；

定 只读语义 为 只读输入.语义（）.快照（）；

测试.断言（只读语义【「状态」】【「只读」】，「输入框应公开只读状态」）；

测试.断言（只读语义【「状态」】【「必填」】，「输入框应公开必填状态」）；

测试.断言（只读语义【「状态」】【「无效」】，「输入框应公开无效状态」）；

测试.断言（包含（只读语义【「操作」】，「选择」），「只读输入框仍应允许选择」）；

测试.断言（包含（只读语义【「操作」】，「复制」），「只读输入框仍应允许复制」）；

测试.断言（非 包含（只读语义【「操作」】，「设置值」），「只读输入框不得声明设置值」）；

测试.断言（非 包含（只读语义【「操作」】，「剪切」），「只读输入框不得声明剪切」）；

测试.断言（非 包含（只读语义【「操作」】，「粘贴」），「只读输入框不得声明粘贴」）；

定 表单路由 为 事件.路由器（）；

表单路由.发送（只读输入，{「类型」：「文本输入」，「文本」：「禁止」}）；

测试.相等（只读输入.取内容（），「初始」）；

只读输入.选择范围（0，2）；

表单路由.发送（只读输入，{「类型」：「按键按下」，「逻辑键」：「Delete」，「修饰键」：{}}）；

测试.相等（只读输入.取内容（），「初始」）；

表单路由.发送（只读输入，{「类型」：「IME组合开始」}）；

表单路由.发送（只读输入，{「类型」：「IME组合更新」，「文本」：「禁止」}）；

测试.断言（非 只读输入.文档值.组合中，「只读输入框不得进入 IME 组合态」）；

测试.断言（非 只读输入.执行无障碍动作（「设置值」，「禁止」），「只读输入框应拒绝辅助技术改值」）；

只读输入.只读（假）；

只读输入.必填（假）；

只读输入.无效（假）；

测试.断言（只读输入.执行无障碍动作（「设置值」，「可编辑」），「可编辑输入框应接受辅助技术改值」）；

测试.相等（只读输入.取内容（），「可编辑」）；

定 可编辑语义 为 只读输入.语义（）.快照（）；

测试.断言（非 可编辑语义【「状态」】【「只读」】，「只读状态应可在运行时关闭」）；

测试.断言（非 可编辑语义【「状态」】【「必填」】，「必填状态应可在运行时关闭」）；

测试.断言（非 可编辑语义【「状态」】【「无效」】，「无效状态应可在运行时关闭」）；

测试.断言（包含（可编辑语义【「操作」】，「设置值」），「可编辑输入框应声明设置值」）；

测试.断言（包含（可编辑语义【「操作」】，「剪切」），「可编辑输入框应声明剪切」）；

表单路由.发送（只读输入，{「类型」：「IME组合开始」}）；

表单路由.发送（只读输入，{「类型」：「IME组合更新」，「文本」：「组合」}）；

测试.断言（只读输入.文档值.组合中，「可编辑输入框应进入 IME 组合态」）；

只读输入.只读（真）；

测试.断言（非 只读输入.文档值.组合中，「切换只读状态应取消现有 IME 组合」）；

言 「控件树集成测试通过」；

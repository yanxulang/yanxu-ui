引「../src/主.yx」为 界面；

引「../src/数据/源.yx」为 数据；

引「标准:测试」为 测试；

令 已见编号：列<数> 为 【】；

令 已见角色：列<文> 为 【】；

法 有键（所据：典，名称：文）：理 则
    归 包含（键列（所据），名称）；
终

法 文本角色（角色名：文）：理 则
    归 包含（【「输入框」，「多行输入框」，「密码框」】，角色名）；
终

法 允许操作（节点：典，操作名：文）：理 则
    定 角色名 为 节点【「角色」】；
    定 状态值 为 节点【「状态」】；
    若 （操作名 等于 「聚焦」） 则
        归 （有键（状态值，「可聚焦」） 且 状态值【「可聚焦」】）；
    终
    若 （操作名 等于 「点击」） 则
        归 包含（【「按钮」，「链接」，「复选框」，「单选框」，「切换」，「标签」，「菜单」，「菜单项」】，角色名）；
    终
    若 （操作名 等于 「设置值」） 则
        归 （文本角色（角色名） 或 包含（【「组合框」，「滑块」，「滚动条」，「分割面板」】，角色名））；
    终
    若 （操作名 等于 「选择」） 则
        归 （文本角色（角色名） 或 包含（【「列表项」，「树项」，「标签」，「菜单项」，「行」，「单元格」，「单选框」】，角色名））；
    终
    若 （操作名 等于 「取消选择」） 则
        归 包含（【「列表项」，「树项」，「标签」，「菜单项」，「行」，「单元格」，「单选框」】，角色名）；
    终
    若 包含（【「复制」，「剪切」，「粘贴」】，操作名） 则
        归 文本角色（角色名）；
    终
    若 包含（【「展开」，「折叠」】，操作名） 则
        归 包含（【「组合框」，「树项」，「菜单」，「菜单项」，「弹出层」】，角色名）；
    终
    若 包含（【「增加」，「减少」】，操作名） 则
        归 包含（【「滑块」，「滚动条」，「分割面板」】，角色名）；
    终
    若 （操作名 等于 「滚动」） 则
        归 包含（【「滚动容器」，「列表」，「树」，「表格」】，角色名）；
    终
    若 （操作名 等于 「滚动到」） 则
        归 真；
    终
    若 （操作名 等于 「显示菜单」） 则
        归 包含（【「按钮」，「输入框」，「多行输入框」，「密码框」】，角色名）；
    终
    归 假；
终

法 审计节点（节点：典） 则
    定 编号值 为 节点【「编号」】；
    测试.断言（（编号值 大于 0），「语义节点编号必须为正整数」）；
    测试.断言（非 包含（已见编号，编号值），「完整语义树不得出现重复编号」）；
    追加（已见编号，编号值）；
    定 角色名 为 节点【「角色」】；
    测试.断言（包含（【「窗口」，「组」，「面板」，「控件」，「文字」，「标题」，「按钮」，「链接」，「复选框」，「单选框」，「切换」，「输入框」，「多行输入框」，「密码框」，「组合框」，「列表」，「列表项」，「树」，「树项」，「标签页」，「标签」，「菜单栏」，「菜单」，「菜单项」，「弹出层」，「对话框」，「滑块」，「进度条」，「滚动条」，「滚动容器」，「分割面板」，「工具栏」，「画布」，「图片」，「表格」，「行」，「单元格」，「分隔符」】，角色名），「控件语义角色必须属于言台 1.0 词汇」）；
    追加（已见角色，角色名）；
    测试.断言（包含（【「空」，「理」，「数」，「文」】，类型（节点【「值」】）），「语义值必须是言台支持的标量」）；
    令 已见操作：列<文> 为 【】；
    逐 操作名 于 节点【「操作」】 则
        测试.断言（非 包含（已见操作，操作名），「单节点不得重复声明无障碍操作」）；
        测试.断言（允许操作（节点，操作名），「控件角色与无障碍操作必须匹配」）；
        追加（已见操作，操作名）；
    终
    逐 子节点 于 节点【「子」】 则
        审计节点（子节点）；
    终
终

法 加载虚拟项（起点：数，数量：数）：列 则
    令 结果：列 为 【】；
    逐 偏移 于 范围（0，数量） 则
        追加（结果，「虚拟审计项」）；
    终
    归 结果；
终

定 根 为 界面.容器（空，「列」，{「间距」：6，「内边距」：8}）；

根.可访问名称（「全控件审计」）；

根.文字（「审计文字」）；

根.按钮（「审计按钮」）；

根.复选框（「审计复选」）；

根.单选框（「审计单选」，「审计组」）；

根.切换（「审计切换」）；

根.下拉选择（【「甲」，「乙」】）；

根.滑块（{「值」：40，「可访问名称」：「审计滑块」}）；

根.进度条（{「值」：60，「可访问名称」：「审计进度」}）；

根.输入框（{「内容」：「单行」，「可访问名称」：「审计输入」}）；

根.多行输入框（{「内容」：「多行」，「可访问名称」：「审计多行输入」}）；

定 滚动 为 根.滚动（{「首选高」：80}）；

滚动.可访问名称（「审计滚动容器」）；

逐 滚动序 于 范围（0，6） 则
    滚动.文字（「滚动审计项」）；
终

定 列表值 为 根.列表（【「列表甲」，「列表乙」，「列表丙」】）；

列表值.可访问名称（「审计列表」）；

列表值.尺寸（240，60）；

定 虚拟源 为 数据.数据源（40，加载虚拟项）；

定 虚拟列表 为 根.虚拟列表（虚拟源，{「首选高」：80，「行高」：24}）；

虚拟列表.可访问名称（「审计虚拟列表」）；

定 标签页值 为 根.标签页（【】）；

标签页值.可访问名称（「审计标签页」）；

标签页值.页（「概览」）.文字（「概览内容」）；

标签页值.页（「设置」）.文字（「设置内容」）；

定 分割值 为 根.分割面板（{「方向」：「横向」，「比例」：0.5，「首选高」：80}）；

分割值.可访问名称（「审计分割面板」）；

分割值.堆叠（{}）.文字（「左侧」）；

分割值.堆叠（{}）.文字（「右侧」）；

定 菜单值 为 根.菜单（【{「标题」：「文件」，「子菜单」：【{「标题」：「打开」}】}，{「标题」：「退出」}】）；

定 弹出值 为 根.弹出层（{「打开」：真，「首选高」：60，「可访问名称」：「审计弹出层」}）；

弹出值.文字（「弹出内容」）；

定 画布值 为 根.画布（{「首选高」：60}）；

画布值.可访问名称（「审计画布」）；

定 图片值 为 根.图片（空）；

图片值.可访问名称（「审计图片」）；

根.布局（【0，0，800，2400】）；

测试.断言（菜单值.打开（），「审计菜单必须可展开」）；

定 完整树 为 根.语义（）.快照（）；

审计节点（完整树）；

逐 角色名 于 【「面板」，「文字」，「按钮」，「复选框」，「单选框」，「切换」，「组合框」，「滑块」，「进度条」，「输入框」，「多行输入框」，「滚动容器」，「列表」，「列表项」，「标签页」，「标签」，「分割面板」，「菜单」，「菜单项」，「弹出层」，「画布」，「图片」】 则
    测试.断言（包含（已见角色，角色名），（「完整控件树缺少语义角色：」 加 角色名））；
终

测试.断言（（长度（已见编号） 不小于 35），「完整控件审计必须覆盖全部控件与集合项」）；

言 「全控件无障碍审计通过」；

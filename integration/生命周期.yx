引「../src/主.yx」为 界面；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

定 应用 为 界面.应用（「言界生命周期测试」）；

定 能力 为 应用.资源能力（）；

测试.断言（能力【「支持」】，「言界必须确认言台资源配额能力」）；

测试.相等（能力【「硬上限」】【「窗口数」】，64）；

定 生效配额 为 应用.配置资源配额（{「资源总数」：4，「窗口数」：1，「计时器数」：1，「图片数」：1，「字体数」：1，「帧字节」：1048576，「无障碍节点」：64，「无障碍文字字节」：16384}）；

测试.相等（生效配额【「资源总数」】，4）；

测试.相等（应用.资源配额（）【「窗口数」】，1）；

定 窗口 为 应用.窗口（{「标题」：「言界生命周期测试」，「宽」：240，「高」：160，「可见」：假}）；

法 忽略计时（原始） 则
终

定 计时任务 为 应用.定时器（1000，真，忽略计时）；

测试.断言（计时任务.运行中（），「托管计时任务应处于运行中」）；

令 计时超限错误 为 「」；

试 则
    应用.定时器（1000，真，忽略计时）；
救 所误 则
    置 计时超限错误 为 所误.消息；
终

测试.相等（计时超限错误，「[PLATFORM_QUOTA_TIMERS] ABI v2 参数无效」）；

测试.断言（计时任务.关闭（），「关闭计时任务应归还平台配额」）；

测试.相等（计时任务.状态（），「已关闭」）；

定 替代任务 为 应用.定时器（1000，真，忽略计时）；

测试.断言（替代任务.取消（），「配额归还后应能创建并取消新任务」）；

测试.相等（替代任务.状态（），「已取消」）；

定 像素字节：字节串 为 字节.从数列（【137，80，78，71，13，10，26，10，0，0，0，13，73，72，68，82，0，0，0，1，0，0，0，1，8，4，0，0，0，181，28，12，2，0，0，0，11，73，68，65，84，120，218，99，100，248，15，0，1，5，1，1，39，24，227，102，0，0，0，0，73，69，78，68，174，66，96，130】）；

定 图片资源 为 应用.加载图片（像素字节）；

测试.相等（图片资源.查询（）【「宽」】，1）；

测试.相等（图片资源.查询（）【「字节数」】，4）；

令 图片超限错误 为 「」；

试 则
    应用.加载图片（像素字节）；
救 所误 则
    置 图片超限错误 为 所误.消息；
终

测试.相等（图片超限错误，「[PLATFORM_QUOTA_IMAGES] ABI v2 参数无效」）；

测试.断言（图片资源.关闭（），「关闭图片资源应归还平台配额」）；

测试.相等（图片资源.状态（），「已关闭」）；

定 替代图片 为 应用.加载图片（像素字节）；

测试.断言（替代图片.关闭（），「配额归还后应能重新加载并关闭图片」）；

测试.相等（替代图片.状态（），「已关闭」）；

令 冻结错误 为 「」；

试 则
    应用.配置资源配额（{「窗口数」：1}）；
救 所误 则
    置 冻结错误 为 所误.消息；
终

测试.相等（冻结错误，「[PLATFORM_QUOTA_LOCKED] ABI v2 参数无效」）；

令 窗口超限错误 为 「」；

试 则
    应用.窗口（{「标题」：「超额窗口」，「可见」：假}）；
救 所误 则
    置 窗口超限错误 为 所误.消息；
终

测试.相等（窗口超限错误，「[PLATFORM_QUOTA_WINDOWS] ABI v2 参数无效」）；

定 窗口关闭报告 为 窗口.关闭（）；

测试.断言（窗口关闭报告【「成功」】，「窗口应完成全部最佳努力关闭步骤」）；

测试.相等（窗口关闭报告【「状态」】，「已关闭」）；

测试.相等（窗口关闭报告【「步骤总数」】，6）；

测试.相等（窗口.关闭（）【「步骤总数」】，6）；

定 托管窗口 为 应用.窗口（{「标题」：「应用托管窗口」，「宽」：240，「高」：160，「可见」：假}）；

定 托管任务 为 应用.定时器（1000，真，忽略计时）；

定 托管图片 为 应用.加载图片（像素字节）；

定 应用关闭报告 为 应用.关闭（）；

测试.断言（应用关闭报告【「成功」】，「应用应完成全部最佳努力关闭步骤」）；

测试.相等（应用关闭报告【「状态」】，「已关闭」）；

测试.相等（应用关闭报告【「步骤总数」】，5）；

测试.相等（托管窗口.生命周期快照（）【「状态」】，「已关闭」）；

测试.相等（托管任务.状态（），「已关闭」）；

测试.相等（托管图片.状态（），「已关闭」）；

测试.相等（应用.关闭（）【「步骤总数」】，5）；

测试.相等（应用.生命周期快照（）【「错误总数」】，0）；

定 关闭后诊断 为 应用.诊断快照（）；

测试.相等（关闭后诊断【「版本」】，2）；

测试.相等（关闭后诊断【「生命周期」】【「状态」】，「已关闭」）；

测试.断言（非 关闭后诊断【「平台可用」】，「关闭后诊断不得访问已释放的言台应用」）；

测试.相等（关闭后诊断【「平台错误」】，空）；

测试.相等（关闭后诊断【「活动图片资源」】，0）；

言 「生命周期配额集成测试通过」；

引「标准:数学」为 数学；

公 类 可观察值 则
    公 域 当前：任意；
    公 域 订阅列：列；
    公 域 下个编号：数；

    公 法 初始化（初值）则
        置 此.当前 为 初值；
        置 此.订阅列 为【】；
        置 此.下个编号 为 1；
    终
    公 法 取（）则 归 此.当前；终
    公 法 设值（新值）则
        定 旧值 为 此.当前；
        若 新值 等于 旧值 则 归；终
        置 此.当前 为 新值；
        逐 项目 于 此.订阅列 则 项目【「回调」】（新值，旧值）；终
    终
    公 法 订阅（回调，立即：理）：数 则
        定 编号 为 此.下个编号；
        置 此.下个编号 为 此.下个编号 加 1；
        追加（此.订阅列，{「编号」：编号，「回调」：回调}）；
        若 立即 则 回调（此.当前，此.当前）；终
        归 编号；
    终
    公 法 取消订阅（编号：数）则
        令 新列：列 为【】；
        逐 项目 于 此.订阅列 则 若 项目【「编号」】不等于 编号 则 追加（新列，项目）；终 终
        置 此.订阅列 为 新列；
    终
终

/// 键值状态容器，适合视图模型与控件属性绑定。
公 类 状态表 则
    公 域 各值：典；
    公 域 各观察：典；
    公 法 初始化（初值：典）则 置 此.各值 为 初值；置 此.各观察 为 {}；终
    公 法 取（名称：文，默认值）则
        若 包含（键列（此.各值），名称）则 归 此.各值【名称】；终
        归 默认值；
    终
    公 法 设值（名称：文，值）则
        置 此.各值【名称】为 值；
        若 包含（键列（此.各观察），名称）则 此.各观察【名称】.设值（值）；终
    终
    公 法 观察（名称：文）：可观察值 则
        若 非 包含（键列（此.各观察），名称）则
            置 此.各观察【名称】为 可观察值（此.取（名称，空））；
        终
        归 此.各观察【名称】；
    终
    公 法 快照（）：典 则 归 此.各值；终
终

公 类 绑定 则
    公 域 来源：任意；
    公 域 订阅编号：数；
    公 域 已关闭：理；
    公 法 初始化（来源：可观察值，应用，转换）则
        置 此.来源 为 来源；置 此.已关闭 为 假；
        法 转发（新值，旧值）则 应用（转换（新值），旧值）；终
        置 此.订阅编号 为 来源.订阅（转发，真）；
    终
    公 法 关闭（）则
        若 非 此.已关闭 则 此.来源.取消订阅（此.订阅编号）；置 此.已关闭 为 真；终
    终
终

法 缓动值（名称：文，进度：数）：数 则
    若 名称 等于「缓入」则 归 进度 乘 进度；终
    若 名称 等于「缓出」则 归 1 减 （1 减 进度）乘（1 减 进度）；终
    若 名称 等于「缓入缓出」则
        若 进度 小于 0.5 则 归 2 乘 进度 乘 进度；终
        归 1 减 数学.幂（减 2 乘 进度 加 2，2）除 2；
    终
    归 进度；
终

/// 与事件循环单调时间配合的无定时器动画值。
公 类 动画 则
    公 域 起值：数；公 域 终值：数；公 域 开始时间：数；公 域 时长：数；
    公 域 缓动：文；公 域 运行中：理；
    公 法 初始化（起值：数，终值：数，开始时间：数，时长毫秒：数，缓动：文）则
        置 此.起值 为 起值；置 此.终值 为 终值；置 此.开始时间 为 开始时间；
        置 此.时长 为 数学.最大（0.001，时长毫秒 除 1000）；置 此.缓动 为 缓动；置 此.运行中 为 真；
    终
    公 法 进度（当前时间：数）：数 则
        定 结果 为 数学.最大（0，数学.最小（1，（当前时间 减 此.开始时间）除 此.时长））；
        若 结果 不小于 1 则 置 此.运行中 为 假；终
        归 结果；
    终
    公 法 值（当前时间：数）：数 则
        定 所进度 为 缓动值（此.缓动，此.进度（当前时间））；
        归 此.起值 加 （此.终值 减 此.起值）乘 所进度；
    终
    公 法 完成（）则 置 此.运行中 为 假；终
终

/// 与平台无关的无障碍语义树节点。
公 类 语义节点 则
    公 域 角色：文；公 域 名称：文；公 域 描述：文；公 域 值：任意；
    公 域 状态：典；公 域 操作：列<文>；公 域 边界：列<数>；公 域 子列：列；
    公 法 初始化（角色：文，名称：文）则
        置 此.角色 为 角色；置 此.名称 为 名称；置 此.描述 为「」；置 此.值 为 空；
        置 此.状态 为 {}；置 此.操作 为【】；置 此.边界 为【0，0，0，0】；置 此.子列 为【】；
    终
    公 法 加子（子：语义节点）则 追加（此.子列，子）；终
    公 法 快照（）：典 则
        令 各子：列 为【】；
        逐 子 于 此.子列 则 追加（各子，子.快照（））；终
        归 {「角色」：此.角色，「名称」：此.名称，「描述」：此.描述，「值」：此.值，
            「状态」：此.状态，「操作」：此.操作，「边界」：此.边界，「子」：各子}；
    终
终
